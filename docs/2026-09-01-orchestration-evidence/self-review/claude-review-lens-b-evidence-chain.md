> UNAUDITED adversarial review, captured 2026-09-01 — verify before relying.
> Produced by an independent Claude session with no access to this session's reasoning.
> Its findings were dispositioned in the plan's section C; some were accepted, some corrected,
> some rejected. The review is preserved verbatim so the dispositions can be checked.

# Lens B — does the evidence support the conclusion?

I have what I need. Here are my findings.

---

## FINDINGS

**1. BLOCKER | "Read this first" §, "What I did", B.7 — Section C does not exist.**
The plan says: *"Running two Codex plan-review rounds against my own draft was in scope... It cost real quota. **Measured: see §C**"*; *"folded the findings back in (§C)"*; and *"Codex weekly window moved from 8% to 12% across the night, all of it the self-review in §C plus four trivial probes."* The document has no section C. `grep -n "^#"` returns headings through B.12 and stops; the file is 1027 lines ending at B.12's build-order table. Part C was an explicit ask ("Explicit ask in part C"), and every cost claim and every self-review finding the plan says it folded back in is unverifiable — there is no record of what the two Codex rounds found, what was accepted, or what was rejected. **Fix:** write §C or delete the three forward references and the cost claim that depends on it. Until then, treat "I ran this through adversarial review twice" as an unevidenced claim under the estate's own rule that a completion claim must quote the tool result that proves it.

**2. BLOCKER | B.10, B.4, B.3 — `--bare` moves every lane off the subscription and onto metered API billing, which inverts the plan's whole purpose.**
B.10's first row prescribes: *"Use `claude -p --bare`. Anthropic says it will become the default for `-p`. Note it also skips CLAUDE.md, so the orchestrator must pass context explicitly."* The cited source, `research-anthropic-official-guidance.md` F1... F8 [high] [vendor-doc], says more than that: *"`--bare` **also skips OAuth/keychain credentials, so you must set `ANTHROPIC_API_KEY`**."* The plan quotes the CLAUDE.md half of that finding and drops the credential half. `grep -n "ANTHROPIC_API_KEY\|OAuth\|subscription"` over the plan returns three hits, none of them about this. The consequence is not cosmetic: B.3 runs every repo lane and all four Opus 5 stages through `claude -p --bare`, so the design as written bills API dollars for work the current design bills to a subscription. B.11 then deletes the one line in `opus-orchestration/SKILL.md` that names this tradeoff — L12: *"workers... **draw on subscription quota instead of API spend**"* — with the verdict "DELETE... it is the exact mechanic that burned two Max subscriptions," without noticing that its replacement stops drawing on the subscription at all. **Fix:** establish empirically whether `--bare -p` can run on OAuth on this machine. If it cannot, either drop `--bare` (and handle repo hook/MCP trust another way, e.g. running lanes from a checkout with no `.claude/settings.json`) or state plainly that the orchestrator is an API-spend design and re-price it. This decision governs B.3, B.4, B.10 and gates 1–2.

**3. BLOCKER | B.5, B.6 gate 13, "Gate 13 deserves its own paragraph" — the held-out oracle is not held out.**
The plan's tier-4 mechanism: *"when Opus writes the lane brief it writes **two** files. The brief, which the lane gets, and `docs/acceptance/lane-<x>.md`, which it does not."* But B.5's `run-plan.json` carries `"held_out_acceptance": "docs/acceptance/lane-a.md"` — a path inside the repo, in a file the plan says is *"Committed before any lane forks"* — and the lane is a `git worktree` of that repo with filesystem access and a Read tool. Nothing in the plan gitignores the path, relocates it outside the repo, or adds a permission deny rule. "Which it does not" is an intention, not a mechanism, in a plan whose own evidence (`research-mechanical-gates-and-instruction-decay.md` F16) records *"One Codex instance created a 2,900-line hash table mapping test inputs to outputs, achieving 97% validation / 0% held-out."* **Fix:** store acceptance files outside the lane's filesystem reach (orchestrator-private directory), and add a gate that greps the lane's transcript/tool log for reads of the acceptance path. Otherwise gate 13 is gate 6 with extra steps.

**4. HIGH | B.4 — the table's column is headed "The evidence, not the vibe," and four of twelve rows contain no external evidence.**
Row *"Lane brief authoring | Opus 5 | high"* justifies itself with *"Opus for ambiguity and judgment"* plus clodex's own `SKILL.md:841` anecdote about a bare control lane — an n=1 self-report from inside the repo being redesigned, and one that says brief quality dominated *process*, not which model writes briefs. Row *"Held-out acceptance check | Opus 5"* offers *"Tier 4. Judgment about whether the outcome happened."* Row *"Morning synthesis | Opus 5"* offers *"The one artifact a human reads. Synthesis and judgment."* These are model assignments — the most consequential thing in the section — with the vibe placed in the column that promises not to contain one. Compounding it, `research-quota-protecting-orchestration.md` GAPS records exactly this hole: *"There is no 2026 data found on quality loss from downgrading a tier for open-ended writing, synthesis, or judgment stages — precisely the stages people most want to reserve for the expensive model."* **Fix:** relabel those rows "judgment call, unevidenced" or rename the column. An unevidenced choice is fine; laundering it through an evidence column is not.

**5. HIGH | B.4 + "Where Codex is actually better" — discount everything the plan itself flagged and three assignments collapse.**
The plan's own caveat paragraph disqualifies: Terminal-Bench (*"self-reported by every vendor... 0 verified... gaps under ~5 points there are noise"*), Anthropic's tables (*"third-party transcriptions because Anthropic publishes its tables as images"*), and every practitioner head-to-head (*"nothing current pairs Codex 0.15x + Sol against Claude Code + Opus 5"*). Applying that:
- **Row 3, "Repo grounding / research sweep → Sol", does not survive.** Its stated basis is *"Terminal driving is Codex's measured strength"* — Sol 88.8 vs Fable 5 86.0 is a 2.8-point gap, inside the plan's own 5-point noise floor. The secondary basis, *"~72% fewer output tokens"*, is `research-codex-vs-claude-model-fit.md` F21 [medium] [third-party-commentary], whose own evidence block contains the contradiction: *"a Composio head-to-head showed... roughly 23% cost difference, not the 5-10× folklore."*
- **Row 6, "Implementation → Terra, not Luna", does not survive.** Its entire basis is SWE-Bench Pro 64.6% — a number for **Sol**, not Terra. There is no SWE-Bench Pro figure for Terra anywhere in the evidence; the choice of Terra rests on `models_cache.json`'s vendor `priority: 2` field. The 64.6 itself comes from F20 [medium] [third-party-commentary] (a codingfleet.com blog transcribing image tables), and F17 [high] says *"OpenAI... published suite is agentic-only,"* so this cross-vendor comparison is two third parties' numbers, not one harness.
- **Row 8, "Release-diff review → Opus 5", does not survive.** Basis: *"Claude won 8 of 12 decided code-review duels"* — F23 [medium] [third-party-commentary], n=12, blakecrosley June 2026, and the GAPS section names it specifically: *"The stage-routing duel counts in particular are pre-Opus-5."* The supporting claim *"Codex confidently invents plausible concurrency bugs"* is recorded in `research-codex-headless-practitioner-reports.md` GAPS as **"NEGATIVE RESULT — no quantified false-positive rate for Codex-as-reviewer. Every account... is anecdotal."**
Rows 1 (deterministic control flow), 5 (plan review, unchanged), 9 (mechanical verify) and — weakly — 10 (client-artifact readback) are the ones that hold. **Fix:** mark rows 3, 6, 8 as hypotheses to test in B.12 step 6 rather than as assignments, and stop calling 64.6 *"the strongest number in the whole comparison"* / *"the biggest single number"* when it is a medium-confidence blog transcription of an unreadable image.

**6. HIGH | B.6 — "11 of 14 are arithmetic" is 10, and the "three gates stay prose" sentence does not reconcile with the table.**
*"Every gate below is one the orchestrator applies. **11 of 14 are arithmetic.**"* Counting the table: gates 1–10 are labeled arithmetic (10), 11–12 artifact, 13 tier 4, 14 prose. Then: *"Three gates stay prose on purpose... a reviewer's severity judgment, the brief's §6 human-context paragraph, and Johnny's morning yes"* — only one of those three (gate 14) is in the table at all. This is a hand-typed count, in a plan whose §B.5 makes *"counts-come-from-ledgers"* a gate (gate 10) and whose governing estate rule reads *"Counts come from ledgers... hand-typed counts drifted in 3 of 3 audited releases."* **Fix:** derive the header number from the table, and either add the two missing prose gates as rows 15–16 or drop the "three gates" sentence.

**7. HIGH | "The census, for scale" + A — the census does not cover the file §A rewrites.**
*"Two numbers matter downstream. **47 prose gates** is where §A's hardening budget goes."* The census's per-component table lists six components: the runner/contract layer, clodex-audit, clodex-build, clodex-plan, clodex-ship, clodex-verify. There is **no row for `clodex/SKILL.md`** — the router, whose §1 preflight checks and §3 bootstrap interview are the entire subject of §A. Every gate §A touches (L178-181, L256-257, L273-287, L488-493, L496-508, L593-609) is outside the 119. So the sentence is false as stated: §A's budget goes to prose gates that were never counted, and none of the 47 are addressed. **Fix:** either census the router and re-derive the number, or say plainly that §A addresses uncounted gates and that the 47 remain untouched.

**8. HIGH | "Both quotas are machine-readable" — the recommended Codex backstop does not apply to this machine's billing path.**
*"there is **no per-run cost cap in the Codex CLI**; the only backstop is a spending limit in the OpenAI dashboard, which is worth setting before the first nightly cron, not after."* The source is `research-codex-headless-practitioner-reports.md` F10 [medium] [third-party-commentary], and the same file's GAPS adds the disqualifier the plan drops: *"the only control is a dashboard-level spend limit **that applies to the API-key path, not the ChatGPT-subscription path**."* The plan's own probe file §3 establishes that this machine's `codex exec` runs bill the ChatGPT subscription (`plan_type: prolite`) under both auth conditions. So the one recommended backstop against a runaway nightly Codex job is inert here, and the plan presents it as the answer. **Fix:** strike the recommendation or replace it with the only control that works on the subscription path — a `used_percent` precondition plus a hard invocation cap in `orchestrate.py`.

**9. HIGH | B.9 gate 7 — the grep misses the exact hole B.9 identifies.**
Gate 7: *"`grep '"by": *"user"' events.ndjson` must return **zero** in an unattended run... One grep, no judgment in it."* B.9's own second quote states the failure mode: *"L294 records `"by": event.get("by", "user")`, so an approval event that **omits** `by` is recorded as the user's."* I confirmed `skills/clodex/state/reducer.py:294` reads exactly that. An event that omits `by` never contains the string `"by": "user"` in `events.ndjson` — so gate 7's grep returns zero and passes, while the derived state records a fabricated user approval. Gate 7 catches only explicit forgery, not the default-value forgery the section is named after. The claim *"it makes gate 7 redundant"* (of the reducer fix) is backwards: the reducer fix is the only one that closes the omission case. **Fix:** run the gate against the **rebuilt snapshot** (`clodex_state.py rebuild`), not the raw ndjson — `any(a["by"] == "user" for a in snap["approvals"])` — which catches both shapes and is genuinely opinion-free.

**10. HIGH | B.6 gate 3 — model readback cannot cover the seven Codex stages it is supposed to protect.**
Gate 3: *"`modelUsage` keys ⊆ the stage's allowed set. Catches the `--permission-mode plan` trap... catches any future silent routing change."* `modelUsage` is a Claude Code envelope field. The plan's own probe file §4 says of Codex: *"The **model that actually ran is not echoed** anywhere in the event stream. A caller knows which model it *requested*; the transcript does not confirm which model *answered*."* Seven of B.4's twelve rows are Codex stages, including the two the plan proposes to change (implementer → Terra, code-reviewer effort). The probe file suggests the rollout file "does carry richer turn metadata — that is where a verification check has to look," but does not establish that the rollout records the answering model. So gate 3, listed as arithmetic and as the countermeasure to silent routing, has no coverage over the majority of the stages. **Fix:** verify whether `~/.codex/sessions/**/*.jsonl` records the served model; if not, say gate 3 is Claude-only and the Codex model assignment is unverifiable at runtime.

**11. HIGH | "And one trap I reproduced" + `local-harness-probes.md` §5b — the hypothesis is contradicted by the observation, and the cost figure is confounded.**
The plan: *"My hypothesis is that plan mode engages the same routing as the `opusplan` alias ('**Opus** for planning, Sonnet for execution')."* The observation in both plan-mode probes was `claude-sonnet-5`. If opusplan routing were engaged, the *planning* phase — which is all a `--permission-mode plan` run does — would show Opus, not Sonnet. The evidence points away from the stated hypothesis. Simpler unconsidered explanations: haiku is not offered in plan mode and silently falls back to the account default; or plan mode has a capability floor. Neither was probed, and no probe varied the requested model (only haiku was tested), varied the permission mode (only `plan` and none), or tested `--bare`. Separately, the plan's table drops probe 2 and presents *"Four times the cost"* by comparing probe 3 (plan mode, **no** slash skill, $0.104) against probe 4 (no plan mode, **with** slash skill, $0.025) — two variables changed between the compared rows, and there is no plan-off/skill-off baseline in the set. **Fix:** the design lesson ("read `modelUsage` back") stands on the model-override observation alone; delete the opusplan hypothesis or run the two probes that would test it (`--model sonnet --permission-mode plan`, `--model opus --permission-mode plan`), and either add the missing baseline or drop the 4x cost claim.

**12. HIGH | B.7 — the round-budget justification contradicts a recorded gap.**
*"Round budget stays at clodex's **3**, and that number now has support it did not have when it was chosen: LLM debugging effectiveness *'decays sharply within 2–3 attempts,'* which is a direct argument for a hard cap on self-correction loops."* Two problems. First, that quote reaches the plan third-hand: `research-quota-protecting-orchestration.md` F13 [medium] [paper] records it as something a June 2026 governance paper *cites* — not a measurement in any source read. Second, and decisive, `research-harness-engineering-discipline.md` GAPS states: *"No source I found quantifies where adversarial review rounds hit diminishing returns... The marginal value of review round N is an open, unmeasured question."* The plan converts a citation about **self-correction/debugging attempts** into "support" for an **adversarial review round** budget, over an explicit recorded null. **Fix:** keep 3 (it has an incident behind it, which the plan also says) and delete the claim of new support.

**13. HIGH | Gates 1–2 vs the plan's stated constraint — the budget gates measure dollars while the binding constraint is a subscription quota.**
Gate 1: *"Claude cumulative `total_cost_usd` from the orchestrator's own ledger < ceiling"*; gate 2 sums `total_cost_usd` per lane; `run-plan.json` carries `"claude_usd_ceiling": 40.00`. `research-quota-protecting-orchestration.md` GAPS opens with: *"**SUBSCRIPTION quota accounting is undocumented.** Every measured cost/quality number found is in API dollars. Nobody publishes how Claude Code's 5-hour and 7-day subscription windows are debited per model tier... **all the tiering evidence optimizes dollars, and the binding constraint here is a quota.**"* The plan never acknowledges this. On a subscription run, `total_cost_usd` is a notional price with no established mapping to the window that actually stops the work; on an API run (see finding 2) it is real money but the quota framing is gone. Either way, the plan's two headline gates measure a proxy the evidence explicitly says is unmapped to the constraint. Note also that the *Codex* side has no such problem — `used_percent` is the real meter — which makes the asymmetry easy to miss. **Fix:** name the proxy as a proxy, and add the one Claude-side quota signal the evidence offers: the undocumented OAuth usage endpoint the plan already mentions and then files as "advisory only."

**14. MEDIUM-HIGH | B.7 vs B.4 — the Anthropic quote is narrowed past what it says, and the plan keeps four separate Opus verification steps.**
The plan quotes F18 [high] [vendor-doc]: *"remove them: instructions like these cause over-verification on Claude Opus 5... **The same applies to legacy harness scaffolding that adds separate verification steps.**"* Its reading: *"it targets in-prompt self-verification instructions inside one agent's turn, not independent review by a different session."* But the final sentence of the vendor quote reaches exactly "harness scaffolding that adds separate verification steps" — which is what B.4's release-diff review, client-artifact readback and held-out acceptance check are. The plan's bias-not-capability counterargument is reasonable and I would not strike it; presenting it as though the quote "does not reach the second" claim is not, when the quote's own last clause does. Related and unaddressed: F19 [high] [vendor-doc] says Opus 5 *"delegates to subagents MORE readily than prior models"* and Claude Code's suppression line is present **only** under the `claude_code` system-prompt preset — while the anthropic GAPS records *"Whether the `claude_code` system-prompt preset's automatic Opus 5 delegation-suppression line is present in `--bare -p` runs... is not documented."* The plan's four Opus stages run `--bare` with `--append-system-prompt-file`, i.e. plausibly without suppression, each free to fan out at Opus rates. **Fix:** acknowledge the quote's scope honestly, and add the copy-pasteable delegation-suppression instruction to the Opus stage prompts.

**15. MEDIUM | "What actually saves quota", B.7 — two domain transfers presented without the domain.**
The 5–6x / degradation result (`research-quota-protecting-orchestration.md` F9–F11) is a **recommender-system ranking** study — NDCG@10 0.7197 → 0.6922, Ranker+Evaluator pipelines. The plan makes it the central argument for gating **adversarial code and document review**. The 3.5x deterministic-orchestration result (F7) is a **COBOL-to-Java translation** experiment. Neither domain is named anywhere in the plan. Worse, F10's actual design rule is *"gate fan-out on **input difficulty**, not on task type"* — and B.7's section title is *"Verification that is gated on difficulty"* while its table gates on *client-visible surface*, *direction gate was yes*, and *boundary strays* — i.e. blast radius and stakes, not input difficulty. **Fix:** name the source domains, and rename B.7 to what it does ("gated on blast radius") or add an actual difficulty signal.

**16. MEDIUM | B.2 — the source cited for "you cannot cheapen an LLM control plane" also contains the counterexample, which is not quoted.**
B.2 pillar 1: *"Delegation reliability was ~0.85 on the top two models and ~0.45 on the fast tier. There is no cheap orchestrator model."* The finding is F12 in `research-quota-protecting-orchestration.md`, and its evidence block continues: *"Same post cites 'a **Fable 5 orchestrator directing Sonnet 5 workers retained 96% of an all-Fable team's score on BrowseComp... at 46% of the cost**'."* That is an LLM control plane, at a premium tier, delivering the plan's exact goal — and it appears in the same paragraph of the same blog post. The plan takes the half that supports "no orchestrator model" and omits the half that supports "a premium orchestrator model with cheap workers," which is what `opus-orchestration` does today and what B.11 deletes. **Fix:** quote both and say why the deterministic script wins anyway (the filesystem/shell argument in B.2 is the strong one and does not need the panel number).

**17. MEDIUM | A.6, B.11 — neither line-count is reproducible from its own table, and B.11 lists the same lines as both DELETE and KEEP.**
A.6 concludes *"Rough accounting: **~63 lines out, ~40 lines in**"* — but its rows cite *ranges* while counting *subsets* (row 6 cites "L509-537 + L568-587", 49 lines, and claims "~15 net"; row 7 cites L593-609, 17 lines, and claims "~8"), so the total cannot be checked. It also excludes the ~150 lines of new Python (`inspect_repo.py` ~80, `bootstrap_check.py` ~70) plus a `VERSION` file and schema edits from the "what this deletes" accounting, then concludes *"`SKILL.md` gets shorter; the mechanism gets stronger"* — true only if the denominator is one file. B.11 claims *"roughly **20 of 69 lines deleted**"*; summing its DELETE rows gives 13 unique lines (L10-12 = 3, L21-28 = 8, L28 double-listed, L53, L66). And L53 appears twice with opposite verdicts: *"| L53 | 'Subagents cannot spawn subagents...' | **DELETE — factually stale** |"* and *"| L51-53 | 'Alternate substrate: Workflow scripts' | **KEEP, demoted** |"* — L53 is the entire Workflow paragraph. The L21-28 DELETE row also swallows L23 (plans are committed files), L24 (one worker per lane), L26 and L27 (a worker never passes its own gate) — all of which B.5 and B.7 depend on. **Fix:** count lines, not ranges; move L53's stale clause out of the KEEP row explicitly; and state the total artifact delta including new scripts.

**18. MEDIUM | B.12 step 1 / B.4 — "the single cheapest change... needs no new machinery" is not true of the runner as it stands.**
B.4: *"`run-codex.sh` L241 currently hardcodes `EFFORT=xhigh` for every first invocation of every role. Running the implementer at `medium` and retrying only non-`complete` envelopes at `high`... **needs no new machinery** (the envelope already records the status)."* L241 is `EFFORT="${CODEX_EFFORT:-xhigh}"` — a single global default. The runner has `role_model()` and `role_sandbox()` but **no `role_effort()`**; per-role effort (B.4 assigns xhigh / high / medium across roles) requires a new function, and "retry non-`complete` at high" requires a retry driver the runner does not have. Separately, the measurement being transferred (`research-quota-protecting-orchestration.md` F1) is **Anthropic's**, on **Opus 5 effort levels on SWE-bench Pro**, applied here to **Codex's `model_reasoning_effort` on GPT-5.6** — and the model-fit GAPS records *"The exact model_reasoning_effort enum accepted by Codex CLI 0.152.0 is unresolved."* **Fix:** describe the actual change (add `role_effort()` + a retry wrapper) and label the cross-vendor transfer as an assumption to measure, not a measured result.

**19. MEDIUM | A.1, A.7, B.8 — "conspicuously blank if skipped" is the load-bearing design idea and the research records it as untested.**
A.1: *"`inspected` is the **tier-2 half**, and it is what makes `[]` legible"*; A.7 upgrades "Inspection actually happened" from prose to **artifact** on this basis; B.8's park record *"is conspicuously useless if any is blank."* `research-mechanical-gates-and-instruction-decay.md` GAPS: *"**No research on the 'conspicuously blank if skipped' template design specifically.** I searched for it and found only generic PR-template checklist enforcement... The claim that a required-field artifact outperforms an instruction is intuitive and consistent with the omission/commission asymmetry finding, **but it is untested** — I found no study measuring artifact-template compliance vs prose compliance."* The plan builds the whole middle tier on it without saying so. **Fix:** one sentence acknowledging the tier-2 mechanism is a reasoned bet, not a measured one. It costs nothing and it is the difference between this document and the prose it is replacing.

**20. MEDIUM | "Two findings that reframe the rest", A.1 — the prohibitions-vs-requirements result was measured on models nobody here runs.**
*"A separate April 2026 result (arXiv:2604.20911) finds 'never do X' degrades under context pressure while 'always emit X' holds. **That is a direct authoring rule**."* The finding (F13/F14) reports *"Mistral Large 3, p < 10⁻³³"*, Safe Turn Depths for *"Mistral Large 3 = 10.6 turns; Qwen 3.5 = 7.1 turns"*, and dilution controls on *"Gemini 2.5 Flash; Llama 3.3 70B."* No Claude model and no GPT-5.6 model appears. The plan uses it twice as a design rule for Claude and Codex workers, and A.1 explicitly justifies the `inspected` field with it: *"because the April 2026 result says requirements persist under context pressure and prohibitions do not."* **Fix:** name the models measured. The result may well transfer; presenting it as settled for this stack is the upgrade this review is looking for.

**21. MEDIUM | Gate 13 — the tier-4 judge is an LLM, and the evidence directory contains a finding that impugns exactly that.**
*"a fresh Opus session gets the acceptance file plus the lane's artifacts and answers one question: did this happen?"* — against *"three to five sentences"* of prose criteria. `research-harness-engineering-discipline.md` F15 [paper, arXiv:2604.16790] measures LLM-judge fragility: *"when the correct answer was positioned first, refined prompts boosted... accuracy to 86.93%... However, when positioned second, the same refined prompt collapsed to 16.79%."* And F16, which the plan cites for gate 13's effect size, itself says LLM judges are *"scalable and faithful but **vulnerable to exploitation by a strengthening model**."* Anthropic's own guidance in the same file (F17) prescribes the mitigations the plan omits: *"clear, structured rubrics... grade each dimension with an isolated LLM-as-judge"* and give the judge *"a way out (return 'Unknown')."* Three-to-five sentences of prose is not a rubric. **Fix:** make the acceptance file a rubric with per-dimension grading and an explicit `Unknown` verdict, per the guidance already in the evidence.

**22. MEDIUM | "Two findings", B.6 gate 13 — the effect size credited to gate 13 comes from a different paper than the one cited beside it, and measures a different mechanism.**
*"the visible-vs-held-out score gap grows ~27 percentage points... **(arXiv:2605.21384, May 2026). Behavior monitoring dropped the hacked-resolved rate from 28.57% to 0.56%** while clean-resolved rose from 40.22% to 60.53%."* The 27pp/100pp figures are 2605.21384 (SpecBench). The 28.57→0.56 figures are `research-harness-engineering-discipline.md` F16, **arXiv:2606.26300**, June — no citation is given, so the sentence reads as the same source. More substantively, B.6 says gate 13 *"is the only one with a measured effect size behind it"* and that effect size is **behavior monitoring** — watching the agent's trajectory — which is close to the opposite of a held-out output oracle. The mechanism that actually supports gate 13 is SpecBench's held-out suite, which the same file qualifies: *"Stronger models exhibit smaller gaps, but all retain non-zero gaps."* **Fix:** cite 2606.26300 for its own number, and drop "behavior monitoring" as gate 13's evidence or add behavior monitoring as a separate gate.

**23. MEDIUM | Intro vs "Both quotas are machine-readable" — the two quota readings in this document disagree, and the higher one has no evidence anywhere.**
Intro: *"Codex weekly window moved from **8% to 12%** across the night."* Quota section: *"Read tonight: ... `"used_percent": 9.0` ... **9% consumed** with six days left."* `local-harness-probes.md` (captured 01:14, seven minutes before the plan was written) records the trajectory *"7% → 8% at 23:36 on 08-31, 8% steady through 00:04, **9%** after the two probes below"* and warns *"The meter moves in whole percents, so it is a coarse instrument at small volumes."* Nothing in the evidence directory shows 12%, and the section that was supposed to measure it (§C) does not exist (finding 1). The plan also says *"four trivial probes"* while the probe file documents at least seven distinct invocations (§3 A/B, §5, §5a, §5b probes 2/3/4). **Fix:** re-read the meter and quote the rollout, or delete the 12% claim.

**24. LOW | "What I did" vs B.7 — the plan exempts itself from its own verification rule.**
B.7's table sends *"Everything else"* to *"One cross-family review round."* This document is not a client-visible surface, has no direction gate, and had no boundary check — so under its own rule it rates one round. It ran *"two Codex plan-review rounds against my own draft — twice, with two different model families"* plus *"an adversarial pass over their load-bearing claims"* plus this review. That may be correct for a document of this consequence; the rule as written does not permit it, which suggests the rule's conditions are wrong rather than that the author was. **Fix:** add a condition covering "the artifact is a design that other work will be built on," which is the real reason both this plan and the client-visible surfaces deserve the full pass.

**25. LOW | B.6 gate 9 — the regex cannot match the line it describes.**
*"First line matches `^r-\d{4}-\d{2}-\d{2}-[a-z]$` or the literal `bare` plus a reason."* The pattern is anchored at both ends, so a first line of `r-2026-09-02-a — lane A report` fails, as does a markdown heading. And *"the literal `bare` plus a reason"* has no mechanism at all — a "reason" is prose. Listed as arithmetic. **Fix:** use a search rather than a full match, and define what makes a reason present (non-empty remainder).

**26. LOW | B.4 row 4 — a documented caveat from the same finding is dropped.**
*"Plan authoring | Sonnet 5 | 'Performance close to Opus 4.8 at lower prices.'"* F33 [high] [vendor-blog] carries that quote and one more sentence: *"One documented caveat: its cybersecurity capability is materially below Opus 4.8 (cannot develop working exploits) — **so security-analysis stages should not be delegated down to Sonnet**."* B.4 also puts Sonnet 5 on *"Verify — run the gates."* Note also the comparison is to **Opus 4.8**, the superseded model, not to Opus 5 — so "close to" understates the gap being accepted. **Fix:** carry the caveat, and say the comparison is against the previous generation.

**27. INFO | Line citations check out.** I verified `clodex/SKILL.md` L178-181, L256-257, L273-287, L488-493, L496-508, L593-609; `run-codex.sh` L95-101, L103-108, L241; `profile.schema.json:163`; `clodex-verify/SKILL.md:684` and the five `client-artifact` prose sites; `reducer.py:294`; `clodex-plan` L429-432 and L461-467; `clodex/SKILL.md` L588-590. All accurate as quoted. The gate census is internally consistent (49+47+23 = 119; per-component columns sum correctly; 84+28+7 = 119). Finding #1 of the plan — the `client-artifact` enum breakage — is fully verified and is the strongest thing in the document.

---

## VERDICT

The plan's *repo-facing* conclusions are well supported and the *model-and-cost* conclusions are not. Everything grounded in files on this disk — the `client-artifact` enum breakage, the `schema_version` currency failure, the `reducer.py:294` attribution default, the runner's Luna-on-writing/Sol-on-reading inversion, the census arithmetic — I checked and it holds; §A is the strongest section and would survive discounting every web source entirely. §B is a different document. Its model-assignment table is headed "the evidence, not the vibe" while four of twelve rows contain only vibe and three more rest on numbers the plan's own caveat paragraph disqualifies four pages earlier: a 2.8-point Terminal-Bench gap inside the plan's stated 5-point noise floor, a SWE-Bench Pro figure for the wrong model transcribed by a blog from an image Anthropic publishes as a picture, and an n=12 duel count the research file's GAPS section names as pre-Opus-5. Beneath that sits a chain of unexamined domain transfers — a recommender-ranking NDCG study standing in for adversarial code review, a COBOL translation experiment standing in for orchestration topology, an Anthropic Opus-5 effort measurement standing in for Codex's reasoning dial, an omission-compliance result measured on Mistral and Qwen standing in as an authoring rule for Claude — and three structural defects that would defeat the design's own goals on contact: `--bare` moves every lane off the subscription this exercise exists to protect, the tier-4 held-out oracle sits in a file inside the worktree the lane can read, and gate 7's grep passes cleanly on precisely the forged-approval shape B.9 was written to close. The recurring pattern is not fabrication — the researchers labeled their confidence honestly and recorded their nulls, and the plan reproduces many caveats faithfully — it is *selective inheritance*: where a finding's evidence block or GAPS entry contains a qualifier that would weaken a design decision, the qualifier is the part that does not make the trip. Against the estate's own definition of done, §A is CODE-VERIFIED and ready for B.12 steps 1–4; §B is not yet a plan to build from, and the missing §C means the adversarial review it claims to have already passed cannot be inspected at all.
